# Release metadata

`release/release.json` is the current release-target authority. Foundation development keeps `channels.github.publish=false` until a maintainer explicitly authorizes publication after all acceptance gates pass.

Release notes under `release/notes/` are drafts until an immutable version tag and GitHub Release are actually published.
